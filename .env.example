# ─── Application Config ───────────────────────────────────────────────

# Server
PORT=8080
NODE_ENV=development
LOG_LEVEL=debug

# ASR Provider (server-managed, not client-controlled)
ASR_PROVIDER=deepgram                    # google | deepgram | openai | qwen-local
ASR_MODEL=nova-3                         # provider-specific model name

# Translation — interim and final are independently configurable
# Providers: google | claude | openai | qwen-local
TRANSLATION_INTERIM_PROVIDER=google
TRANSLATION_INTERIM_MODEL=nmt            # google: nmt|tllm, claude/openai: model id
TRANSLATION_FINAL_PROVIDER=google
TRANSLATION_FINAL_MODEL=tllm             # google: nmt|tllm, claude/openai: model id

# Local translation model (Qwen3-30B-A3B via vLLM / vLLM-Metal)
QWEN_TRANSLATION_URL=http://localhost:8002/v1
QWEN_TRANSLATION_MODEL=Qwen/Qwen3-30B-A3B

# Qwen3-ASR (self-hosted)
QWEN3_ASR_ENABLED=false
QWEN3_ASR_HOST=qwen3-asr
QWEN3_ASR_PORT=8001

# Frontend
VITE_WS_URL=ws://localhost:8080/ws

# ─── Credentials & Keys ──────────────────────────────────────────────

# Google Cloud
GOOGLE_APPLICATION_CREDENTIALS=/secrets/google-creds.json
GOOGLE_TRANSLATION_PROJECT_ID=
GOOGLE_TRANSLATION_LOCATION=us-central1  # required for google-tllm

# Deepgram
DEEPGRAM_API_KEY=

# OpenAI
OPENAI_API_KEY=

# Anthropic (Claude)
CLAUDE_API_KEY=
